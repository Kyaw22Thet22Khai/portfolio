# Vite environment variables (client-exposed)
# Copy this file to `.env` and fill in your values.
# Do NOT commit `.env` to Git — it's ignored via .gitignore.

# Formspree form ID, e.g. xyzabcd
VITE_FORMSPREE_ID=mojqaogz

